import { CategoryEntity } from "../entities/category.entity";

export const categoryMock: CategoryEntity = {
    id: 1,
    name: 'Category Mock',
    createdAt: new Date(),
    updatedAt: new Date(),
}